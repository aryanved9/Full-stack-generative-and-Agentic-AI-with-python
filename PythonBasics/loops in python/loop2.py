# A chai shop makes tea in batches every 15 min, you want to simulate 4 batches.
# Task use range() to simulate batch numbers.

#  use range to simulate batch number and print preparing chai for batch #number.

for batches in range(1,5):
    print(f"preparing chai for batch #{batches}")